{
  "name": "reason-react-set-game",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bs-loader": "^2.0.7",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "reason-scripts": "0.9.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "prepare": "npm link bs-platform"
  },
  "devDependencies": {
    "bs-jest": "^0.3.2",
    "reason-react": "^0.7.0"
  }
}
